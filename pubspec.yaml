name: nextlearn
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cloudinary_flutter: ^1.0.0 # Use the latest version
  cloudinary_url_gen: ^1.0.0 # Use the latest version
  video_player: ^2.9.1
  chewie: ^1.7.5

  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  shared_preferences: ^2.2.3
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.4
  firebase_auth: ^4.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
